# Core runtime dependencies
ollama==0.6.2
qdrant-client==1.18.0
sympy==1.14.0
# Versions below verified against PyPI on 2026-05-22 and switched to >= for
# flexibility. requests/pydantic/pytest/opencv were already valid current
# releases; black and numpy were bumped to the latest published versions.
requests>=2.34.2
pydantic>=2.13.4
pydantic-settings==2.14.1
psutil==7.2.2
structlog==25.5.0

# Runtime deps that are imported but were previously undeclared
PyYAML>=6.0            # memory/nexus/server.py + graph.py (frontmatter parsing)
SQLAlchemy>=2.0        # db/school_models.py + school agents (classroom/calendar/powerschool/project_context)
beautifulsoup4>=4.12   # agents/web_search_agent.py (DuckDuckGo HTML fallback parsing)
playwright>=1.40       # agents/powerschool_agent.py (headless browser automation)

# Google API client — imported by agents/calendar_agent.py + classroom_agent.py
# (_get_service uses google.oauth2, google_auth_oauthlib, googleapiclient).
# Previously undeclared; the agents raised a RuntimeError telling the user to
# pip install these. Now declared so a clean install includes them.
google-api-python-client>=2.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.0

# Development tools
pytest>=9.0.3
pytest-asyncio>=0.23   # tests/test_conductor_init.py uses @pytest.mark.asyncio
black>=26.5.1
flake8==7.3.0
pip-tools>=7.4         # `pip-compile requirements.txt` regenerates requirements.lock

# Optional extras (only required for extended image or GPU tooling)
aiofiles==25.1.0
GPUtil==1.4.0
pywebview==6.2.1
opencv-python>=4.13.0.92
pyautogui==0.9.54
# OCR: pytesseract only. easyocr was removed — it pulls in the full PyTorch
# stack (~2GB) and neither OCR lib is imported today (visual_scanner_agent does
# text extraction via the vision LLM, not a Python OCR package). pytesseract is
# kept as the lightweight option if/when local OCR is wired in.
pytesseract==0.3.13
mss==10.2.0
numpy>=2.4.6

# Optional — only needed to call Conductor.start_task() from inside an already
# running event loop (e.g. Jupyter / async REPL); see orchestration/conductor.py
nest_asyncio>=1.5

# Overlay HUD dependencies
aiohttp>=3.9.0
faster-whisper>=1.0.0
sounddevice>=0.4.6

# Study mode — document reading and write-back
PyMuPDF>=1.24.0
python-docx>=1.1.0

# ZIM knowledge base (memory/zim_kb/) — offline retrieval over OpenZIM archives.
libzim>=3.10.0
markdownify>=1.2.0     # HTML -> Markdown for converter.py
